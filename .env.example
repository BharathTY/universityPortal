# PostgreSQL (Prisma). In development, cap connections to avoid "too many clients" on hot reload:
# DATABASE_URL="postgresql://...?schema=public&connection_limit=5"
DATABASE_URL="postgresql://postgres:TestAdmin@103.182.211.219:5432/university_portal?schema=public"

# JWT signing (minimum 32 characters)
JWT_SECRET="change-me-to-a-long-random-secret-at-least-32-chars"

# Session cookie: in production, `secure` defaults true (requires HTTPS). If you only have HTTP (e.g. http://IP:7777), set:
# COOKIE_SECURE=false

# Canonical site URL for server-generated links (invite emails, counsellor invites). Prefer this in production.
# No trailing slash. Example: https://portal.example.edu or http://103.182.211.219:7777
APP_BASE_URL=http://103.182.211.219:7777

# Public URL for the browser (no trailing slash). Often matches APP_BASE_URL; required for client-side absolute URLs if used.
NEXT_PUBLIC_APP_URL="http://103.182.211.219:7777"

# Default role slug for first-time sign-in (must exist in DB / seed)
DEFAULT_ROLE_SLUG=student

# Sign-in: omit or set false for email-only (no 6-digit code). Set true to require OTP / email verification flow.
# REQUIRE_OTP_LOGIN=true

# In development, OTP is also returned by the API and shown on the login / verify screens.
# In production, set to "true" only for demos/staging (never expose OTP publicly on real sites).
# SHOW_OTP_ON_SCREEN=false

# Show demo account shortcuts on /login (still requires seeded DB). Default: on in development only.
# NEXT_PUBLIC_SHOW_DEMO_LOGINS=true

# SMTP (optional — in development OTP is logged to the console if unset)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=register@qspiders.com
SMTP_PASS=rzuk sdqk pcqm gqix
EMAIL_FROM=register@qspiders.com

# Optional (recommended if your SMTP provider requires it)
# SMTP_SECURE=false           # set true for port 465
# SMTP_REQUIRE_TLS=true       # for providers that insist on STARTTLS

# Seed / demo accounts — full table: docs/DEMO_LOGINS.md
# Master: master@university.local | Admin: admin@university.local | University: university@university.local
# Counsellor: counsellor@university.local | Consultant: consultant@university.local
# Students: student@ … student6@ @university.local (see doc for counsellor vs consultant link)
SEED_COUNSELLOR_EMAIL=counsellor@university.local
SEED_CONSULTANT_EMAIL=consultant@university.local

# Comma-separated origins/hosts — merged into next.config.ts `allowedDevOrigins` (dev HMR over LAN/public IP).
ALLOWED_DEV_ORIGINS=http://103.182.211.219:7777

# Razorpay — online payment on consultant Card/Online tab and student checkout when both are set.
# RAZORPAY_KEY_ID=rzp_test_xxxx
# RAZORPAY_KEY_SECRET=xxxx
# NEXT_PUBLIC_RAZORPAY_KEY_ID=rzp_test_xxxx

# Payment split — university vs QSpiders (must sum to 100)
PAYMENT_UNIVERSITY_SHARE_PERCENT=70
PAYMENT_QSPIDERS_SHARE_PERCENT=30
PAYMENT_PLATFORM_LABEL=QSpiders

# Razorpay Route — auto-transfer university share on online payments (optional)
# Requires Razorpay Route + linked account for each university (or global fallback below)
# RAZORPAY_ROUTE_SPLIT_ENABLED=true
# RAZORPAY_UNIVERSITY_LINKED_ACCOUNT_ID=acc_xxxx

# UPI ID on consultant collect-payment QR — prefer university.paymentUpiId in Master Admin
# NEXT_PUBLIC_COLLECT_UPI_ID=yourname@upi

# External university details API (optional — falls back to master catalog when unset)
# UNIVERSITY_DETAILS_API_URL=https://api.example.com/universities/{id}
# UNIVERSITY_DETAILS_API_KEY=your-api-key
# UNIVERSITY_DETAILS_API_TIMEOUT_MS=8000

# QSpiders program catalog API (qualification / degree / stream dropdowns on Add University)
# QSPIDERS_PROGRAM_CATALOG_API_URL=https://api.example.com/program-catalog
# QSPIDERS_PROGRAM_CATALOG_API_KEY=your-api-key
# QSPIDERS_PROGRAM_CATALOG_API_TIMEOUT_MS=8000

# QSpiders → Eduversity webhook (auto-sync universities & catalog data)
# QSPIDERS_WEBHOOK_ENABLED=true
# QSPIDERS_WEBHOOK_SECRET=long-random-secret-for-hmac-sha256
# Configure QSpiders to POST: https://your-portal/api/webhooks/qspiders
# Header: X-QSpiders-Signature: sha256=<hmac-hex-of-raw-body>

# MOU SPOC details — comma-separated inbox(es) for Sheshu sir team notifications
# SHESHU_TEAM_EMAIL=sheshu.team@example.com